# Development / test dependencies for the AI Red Team CLI.
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
-r requirements.txt
pytest>=8.0
reportlab>=4.0      # exercised by tests/test_reporter.py (--pdf path)
openpyxl>=3.1       # roadmap (.xlsx) parsing in docs/
python-docx>=1.1    # roadmap (.docx) parsing in docs/
fastapi>=0.110      # --serve REST API (server.py)
httpx>=0.27         # fastapi TestClient (tests/test_server.py)
uvicorn>=0.29       # --serve runtime (optional; server.run())
# playwright        # --schema browser (optional; pip install playwright && playwright install chromium)
